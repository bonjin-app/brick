-- 목록용 썸네일 주소.
--
-- 운영자가 고르는 대표 사진은 미디어의 **원본** 주소다(업로드 때 2400px 로 줄이긴 한다).
-- 그런데 목록·검색은 그 사진을 64~300px 칸에 그린다 — 상품 24개가 깔린 첫 화면이 수 MB 가
-- 되고, 모바일에서 특히 아프다. 그래서 저장할 때 대응하는 썸네일 주소를 함께 적어 두고
-- 목록은 그것을 쓴다. 없으면(외부 URL·GIF·SVG) 원본을 쓰므로 화면은 깨지지 않는다.
ALTER TABLE shop_products
  ADD COLUMN IF NOT EXISTS thumb_url text;
