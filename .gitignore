node_modules/
dist/
.next/
.next-dev/
.turbo/
*.log
.env
.env.local
.env.*.local
coverage/
.DS_Store

# 런타임 데이터 (설치 후 생성)
/data/
/uploads/
*.tsbuildinfo
/uploads/
/.local-uploads/
/dist-release/
/data/

# 로컬 도구 설정 (개발자별)
.claude/
.playwright-mcp/

# 개발용 PostgreSQL 데이터 (pnpm db:dev)
.dev-db/

# Next 가 빌드할 때마다 다시 쓴다 — 그 안의 경로가 distDir 을 따라간다.
# 개발 서버(.next-dev)와 프로덕션 빌드(.next)가 번갈아 고쳐 쓰므로, 커밋해 두면
# 작업 트리가 한쪽을 돌릴 때마다 더러워진다. 파일 자체는 빌드가 다시 만든다.
apps/web/next-env.d.ts
